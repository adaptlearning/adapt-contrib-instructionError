// Add to course.json
"_instructionError": {
    "_isEnabled": true,
    "_showAsPopup": false,
    "title": "Instructions",
    "body": "{{#if instruction}}{{{instruction}}}{{else}}Please complete the question and then select Submit.{{/if}}"
}
